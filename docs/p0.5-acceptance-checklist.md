# P0.5 Image Intake — Acceptance Checklist

## I1 · Storage Bucket + Schema

- [ ] `intake-images` bucket exists in Supabase Storage (private)
- [ ] RLS policies: farmer can INSERT/SELECT/DELETE only their own files
- [ ] `orders.raw_image_urls` column exists (text[])
- [ ] `orders.image_quality` column exists (text)

## I2 · Vision Parse Service

- [ ] `/api/parse/image` accepts `{ imageStoragePaths: string[] }`
- [ ] Returns full `ParsedOrderDraft` including `image_quality` and `ocr_text`
- [ ] `overall_confidence = min(parse_confidence, image_quality_factor)`
- [ ] Quality factors: clear=1.0, blurry=0.6, partial=0.4, unreadable=0
- [ ] JSON extraction handles code-fenced and bare responses
- [ ] Failed download returns 403 (cross-farmer access blocked)

## I3 · Upload UI

- [ ] `/intake` has 貼上文字 / 上傳截圖 tab switcher
- [ ] Drop zone accepts JPG, PNG, WebP
- [ ] 拍照上傳 button triggers camera on mobile
- [ ] Canvas resize caps images at 2000px, JPEG 0.85 quality
- [ ] Upload to `/api/intake/upload-image` succeeds
- [ ] `saveOrderDraft` sets `intake_mode = 'image'` and `raw_image_urls`

## I4 · Multi-image & Confidence UX

- [ ] Max 5 images enforced with toast on excess
- [ ] Drag-to-reorder preview grid works (dnd-kit sortable)
- [ ] Confidence bar shown in draft editor
- [ ] Amber banner for confidence 0.5–0.8
- [ ] Red banner + "改用文字模式" for confidence 0.3–0.5
- [ ] `< 0.3` confidence → UnreadableErrorScreen with 3 buttons
- [ ] `ImagePreviewSection` collapsible card shows signed-URL thumbnails
- [ ] Thumbnail tap opens full-size modal lightbox

## I5 · Order Detail Integration

- [ ] Order detail header shows intake mode label (貼上文字 / 圖片上傳 / 手動建立 / LINE Webhook)
- [ ] Image-intake orders show collapsible 上傳的截圖 gallery with signed URLs
- [ ] Raw message section title shows "OCR 辨識文字" for image orders
- [ ] Order card shows intake_mode icon (FileText / ImageIcon / Pencil) with tooltip
- [ ] `/intake` default tab auto-selects 上傳截圖 on mobile (pointer: coarse)
